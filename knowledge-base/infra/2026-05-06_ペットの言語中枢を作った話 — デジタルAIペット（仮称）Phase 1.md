---
title: "ペットの言語中枢を作った話 — デジタルAIペット（仮称）Phase 1"
url: "https://zenn.dev/asobu/articles/7b92f6cdf954bd"
date: 2026-05-06
tags: [llama.cpp, Qwen2.5, GGUF, Q4_K_M, Android, SQLCipher, Argon2id, Jetpack Compose, Room, Koin]
category: "infra"
related: [407, 3653, 647, 2862, 399]
memo: "[Zenn LLM] ペットの言語中枢を作った話 — デジタルAIペット（仮称）Phase 1"
processed_at: "2026-05-06T12:22:44.713764"
---

## 要約

AndroidアプリにローカルLLMを組み込むPhase 1の実装記録。Qwen 2.5 3B InstructをQ4_K_Mに量子化したGGUFモデルを端末上で動作させ、Compose UIでストリーミング応答を表示し、SQLCipherで暗号化したローカルDBに対話履歴を永続化するまでを実装した。

llama.cppのAndroidバインディングとしてMaven Central上の`com.llamatik:library-android:1.1.0`（約44MiB、4 ABI分のsoファイル入り）を採用。Plan A（既製バインディング）/Plan B（JNI自前実装）/Plan C（公式Kotlin wrapper）の3択に対し、20分のタイムボックスを切ってPlan Aを試したところ9分で動作確認できた。READMEに`LlamaBridge`のAPIシグネチャが記載されていなかったため、AARをjarとして解析し`javap -p`で全シグネチャを書き出して確定させた。`generateStream`はコールバックインターフェース（`onDelta`/`onComplete`/`onError`）経由で`callbackFlow`にラップし`Dispatchers.IO`で実行する設計とした。

Qwen 2.5のChatMLフォーマットでプロンプトを組み立て、`<|im_start|>assistant\n`で止めてモデルに続きを生成させる方式を採用。3つのユニットテストでフォーマット崩れがないことを確認した。

暗号化基盤はPhase 0で構築したArgon2id（m=64MiB, t=2, p=4）→Android Keystore→SQLCipherの三層構造を継承。起動時のパスフレーズ入力からARgon2idによる32バイト鍵導出、SQLCipherのDBオープン、鍵バイト列のゼロ埋め消去までを一気通貫で実装した。なおKeystore封緘した暗号文の再復号フロー（生体認証経路）はPhase 7の宿題として未実装。

Room DB移行ではAGP 9.x＋KSP環境で`@AutoMigration`が`room.schemaLocation`を要求する問題に遭遇。スキーマJSONをリポジトリにコミットすることへの整合性上の懸念から、手動`Migration`を選択した。

Koin 4.0のCompose連携では「`koin-androidx-compose`が廃止されKMP向けartifactに移行した」という誤情報をAIが生成し、著者自身も一度信じてしまったと記録している。実際にはKMP向けの並列パッケージが新設されたにすぎず、Android専用プロジェクトでは`koin-androidx-compose`が依然として推奨経路であることをMaven Centralで確認して訂正した。

サブエージェント駆動開発により22タスク・27コミットを数時間で消化した一方、「自分が何を承認したか覚えていない」というレビュー負債の問題も率直に指摘している。監査エージェント開発への示唆として、AIによるコード生成の高速化と引き換えに生じるレビュー品質の低下は、自動化監査システム設計においても同様のリスクとして認識すべき点である。

## アイデア

- AARをjavapで逆コンパイルしてAPIシグネチャを確定する手法は、ドキュメント不足のライブラリに対する実践的なリバースエンジニアリングアプローチとして応用可能
- AIが生成した技術的事実（Koin 4.0のartifact廃止）を著者自身が一度信じてしまい、別のAI（Gemini）のレビューで誤りを発見したという二重のAI活用による品質保証パターンは興味深い
- サブエージェント駆動開発で高速に実装できる一方、レビュー負債（何を承認したか把握できない）が蓄積するというトレードオフは、エージェント設計における人間の監督範囲の設計問題として普遍的

## 前提知識

- **llama.cpp** → /deep_940 Llama 3.2 リリース：視覚理解とオンデバイス推論を兼ね備えたオープンモデル群
- **GGUF量子化** (TODO: 読むべき)
- **ChatMLフォーマット** (TODO: 読むべき)
- **SQLCipher** (TODO: 読むべき)
- **Android JNI/NDK** (TODO: 読むべき)

## 関連記事

- /deep_407 API vs ローカルLLM、感覚で選ぶのをやめるための判断フレームワーク
- /deep_3653 システムダイナミクスAIアシスタントのベンチマーク：クラウドLLM対ローカルLLMによるCLD抽出・議論タスク評価
- /deep_647 Transformersライブラリ：モデル定義の標準化とエコシステムの統合
- /deep_2862 Qwen3-235B-A22B を OpenCode と Ollama でローカル運用する超初心者向けガイド
- /deep_399 OpenClawエージェントをオープンモデルに移行する方法

## 原文リンク

[ペットの言語中枢を作った話 — デジタルAIペット（仮称）Phase 1](https://zenn.dev/asobu/articles/7b92f6cdf954bd)
